[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Pre-deploy disabled - using Custom Start Command in Railway dashboard instead
# preDeployCommand = "/bin/sh -c \"psql $DATABASE_URL -f migrations/001_create_tables.up.sql && ./seed\""
